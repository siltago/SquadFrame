# SquadEngineer AutoCAD

Integração baseada no protótipo validado. Mantém a paleta, os comandos `SQUADENGINEERING`/`SQUADSTATUS`, o evento `SaveComplete`, revisões `R0001...`, JSON e armazenamento em `Documentos\SquadSystem\SquadEngineering\Versoes`.

Compile informando a instalação do AutoCAD: `dotnet build -c Release -p:AcadInstallDir="C:\Program Files\Autodesk\AutoCAD 2026"`.

