// Shell do módulo SquadFlow. Layout simples — não herda header/sidebar do SquadFrame.
export default function SquadFlowLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
