// Shell do módulo SquadStock. Layout simples — não herda header/sidebar do SquadFrame.
export default function SquadStockLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
