"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { Button, Textarea } from "@/ui"; import { reviewVisitAction } from "../../actions";
export function ReviewForm({visitId}:{visitId:string}){const router=useRouter();const[error,setError]=useState("");async function submit(fd:FormData){const result=await reviewVisitAction({id:visitId,decisao:fd.get("decisao"),comentario:fd.get("comentario")});if(!result.ok)setError(result.erro);else router.push(`/squadmeasure/visitas/${visitId}`);}return <form action={submit} className="card space-y-3 p-5"><h2 className="font-bold">Decisão da revisão</h2><Textarea required name="comentario" placeholder="Registre o parecer técnico" rows={4}/>{error&&<p className="text-sm text-danger">{error}</p>}<div className="flex flex-wrap gap-2"><Button name="decisao" value="aprovar" type="submit" variant="success">Aprovar</Button><Button name="decisao" value="solicitar_correcao" type="submit" variant="warning">Solicitar correção</Button></div></form>}
