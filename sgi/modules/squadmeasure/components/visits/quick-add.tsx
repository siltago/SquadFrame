"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { Button, Input, Select, Textarea } from "@/ui"; import { createElementAction, createEnvironmentAction, createMeasurementAction, createObservationAction } from "../../actions";
export function QuickAdd({visitId,environments}:{visitId:string;environments:Array<{id:string;nome:string;elementos:Array<{id:string;nome:string}>}>}){const router=useRouter();const[msg,setMsg]=useState("");async function submit(fd:FormData){setMsg("");const kind=fd.get("kind");let result;if(kind==="environment")result=await createEnvironmentAction({visita_id:visitId,nome:fd.get("nome"),sequencia:environments.length,status:"pendente"});else if(kind==="element")result=await createElementAction({ambiente_id:fd.get("ambiente_id"),nome:fd.get("nome"),tipo:fd.get("tipo"),quantidade:1,sequencia:0,status:"pendente",requer_atencao:false});else if(kind==="measurement")result=await createMeasurementAction({elemento_id:fd.get("elemento_id"),tipo:fd.get("tipo"),nome:fd.get("nome"),valor:fd.get("valor"),unidade:fd.get("unidade"),origem:"manual",estado:"provisoria"});else result=await createObservationAction({visita_id:visitId,categoria:fd.get("categoria"),texto:fd.get("texto"),importante:false});if(!result.ok)setMsg(result.erro);else{(document.getElementById("quick-add") as HTMLFormElement)?.reset();router.refresh();}}
return <form id="quick-add" action={submit} className="card space-y-3 p-4"><p className="font-semibold">Registro rápido</p><Select name="kind" defaultValue="environment"><option value="environment">Ambiente</option><option value="element">Elemento</option><option value="measurement">Medida</option><option value="observation">Observação</option></Select><Input name="nome" placeholder="Nome (ambiente, elemento ou medida)"/><Select name="ambiente_id"><option value="">Ambiente</option>{environments.map(e=><option value={e.id} key={e.id}>{e.nome}</option>)}</Select><Select name="elemento_id"><option value="">Elemento</option>{environments.flatMap(e=>e.elementos).map(e=><option value={e.id} key={e.id}>{e.nome}</option>)}</Select><div className="grid grid-cols-2 gap-2"><Select name="tipo"><option value="janela">Janela / largura</option><option value="porta">Porta</option><option value="altura">Altura</option><option value="profundidade">Profundidade</option><option value="personalizada">Personalizado</option></Select><Input name="valor" type="number" step="any" placeholder="Valor"/></div><Select name="unidade" defaultValue="mm"><option>mm</option><option>cm</option><option>m</option><option>graus</option><option>unidade</option></Select><Select name="categoria" defaultValue="informacao"><option value="informacao">Informação</option><option value="atencao">Atenção</option><option value="impedimento">Impedimento</option><option value="risco">Risco</option><option value="revisao_necessaria">Revisão necessária</option></Select><Textarea name="texto" placeholder="Texto da observação"/>{msg&&<p className="text-sm text-danger">{msg}</p>}<Button type="submit" fullWidth>Adicionar</Button></form>}
